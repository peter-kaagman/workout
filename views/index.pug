extends layout

block content
  h1= title
  p Welcome to #[em Workout Tracker]; A trial to make a training app.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The Workout Tracker has the following record counts:

    ul
      li #[strong Trainings: ] !{data.training_count}
      li #[strong Muscles: ] !{data.muscle_count}
